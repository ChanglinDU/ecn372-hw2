# Keep this directory in git, but ignore raw data files (including grader `test.csv`).
*
!.gitignore

# Keep this directory in git, but ignore raw data files (including grader `test.csv`).
*
!.gitignore

